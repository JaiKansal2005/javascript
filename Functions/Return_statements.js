//Return statements specify the value that a function should return when it is called. It allows functions to compute a value and send it back to the caller.

function add(a,b){
    return a+b;
}

let sum =add(2,3);
console.log("The sum is "+sum)