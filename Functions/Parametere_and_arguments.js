//Parameters are placeholders for values that a function will receive when it is called. Arguments are the actual values passed into a function when it is invoked.

function greet(name){
    console.log("Hello",name);
}
greet("Jai");