/*    Initialize a variable count with a value of 5.
    Write a while loop that decrements count by 1 on each iteration and prints the current value of count to the console until count reaches 0. */

    let count=5;

    while(count>=0){
        console.log(count);
        count--;
    }